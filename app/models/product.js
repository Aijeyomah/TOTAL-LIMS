import db from "./../db";
import productQuery from "./../db/queries/product/products";
import blendQuery from "./../db/queries/product/blend";

/**
 * Contains a schema that describes the staff resource on the app.
 * @class productModel
 *
 */
class productModel {
    constructor() {
        this.
    }
}
